# Dependencies
node_modules/

# Prebuilt extension (extension/) is intentionally committed so users can
# clone and Load unpacked without a build step. Regenerate with `npm run build`.

# Packaged extension archives (run `npm run package` to regenerate)
*.zip

# macOS
.DS_Store

# Editor
.vscode/
.idea/

# Logs
*.log
